# Changelog

## Unreleased

- `ToonFu.encode` and `ToonFu::Encoder` encode `nil`, booleans, integers, floats and strings per TOON spec 4.1, with `delimiter:` (`","`, `"\t"`, `"|"`).

## 0.0.1

Name claimed. No encoder yet.
